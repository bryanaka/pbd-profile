require 'spec_helper'

feature "Scientists Backbone App#index - scientist grid view" do

	context "when logged in as an unconfirmed user" do
		
		scenario "user views the grid"

		scenario "user tries to edit a scientist"
					
	end

	context "when logged in as a scientist" do
	end

	context "when logged in as an admin" do
	end

	context "when logged in as a superadmin" do
	end
	
end
